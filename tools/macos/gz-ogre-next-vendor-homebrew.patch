diff --git a/CMakeLists.txt b/CMakeLists.txt
index a904707..dd226cd 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -4,6 +4,13 @@ project(gz_ogre_next_vendor)
 find_package(ament_cmake REQUIRED)
 find_package(ament_cmake_vendor_package REQUIRED)
 
+# Homebrew supplies the Ogre 2.3 ABI used by its Gazebo Harmonic bottles.
+if(APPLE)
+  find_package(PkgConfig REQUIRED)
+  pkg_check_modules(OGRE_NEXT_HOMEBREW REQUIRED OGRE-2.3>=2.3.1)
+  set(OGRE_NEXT_HOMEBREW_FOUND TRUE)
+endif()
+
 set(EXTRA_CMAKE_FLAGS)
 if(CMAKE_SYSTEM_PROCESSOR MATCHES "^(aarch64.*|AARCH64.*|arm64.*|ARM64.*|arm.*|ARM.*)")
   # Flags for non SIMD architectures
@@ -12,6 +19,7 @@ if(CMAKE_SYSTEM_PROCESSOR MATCHES "^(aarch64.*|AARCH64.*|arm64.*|ARM64.*|arm.*|A
 endif()
 
 ament_vendor(${PROJECT_NAME}
+  SATISFIED ${OGRE_NEXT_HOMEBREW_FOUND}
   VCS_URL https://github.com/OGRECave/ogre-next.git
   VCS_VERSION v2.3.3
   CMAKE_ARGS
