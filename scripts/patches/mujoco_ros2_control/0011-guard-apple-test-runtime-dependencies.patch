From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: "zjumty@aliyun.com" <zjumty@aliyun.com>
Date: Fri, 14 Aug 2026 16:34:35 +0800
Subject: [PATCH 11/11] guard Apple test runtime dependencies


diff --git a/mujoco_ros2_control/tests/CMakeLists.txt b/mujoco_ros2_control/tests/CMakeLists.txt
index 8c25f49..af96561 100644
--- a/mujoco_ros2_control/tests/CMakeLists.txt
+++ b/mujoco_ros2_control/tests/CMakeLists.txt
@@ -1,58 +1,59 @@
 find_package(ament_cmake_gtest REQUIRED)
 find_package(ament_cmake_pytest REQUIRED)
-find_package(rcl_logging_spdlog REQUIRED)
-find_package(rmw_fastrtps_cpp REQUIRED)
-find_package(rmw_dds_common REQUIRED)
-find_package(backward_ros REQUIRED)
-get_filename_component(RCL_LOGGING_SPDLOG_PREFIX
-  "${rcl_logging_spdlog_DIR}/../../.." ABSOLUTE)
-set(RCL_LOGGING_SPDLOG_LIBRARY_DIR "${RCL_LOGGING_SPDLOG_PREFIX}/lib")
-get_filename_component(RMW_FASTRTPS_CPP_PREFIX
-  "${rmw_fastrtps_cpp_DIR}/../../.." ABSOLUTE)
-set(RMW_FASTRTPS_CPP_LIBRARY_DIR "${RMW_FASTRTPS_CPP_PREFIX}/lib")
-get_filename_component(RMW_DDS_COMMON_PREFIX
-  "${rmw_dds_common_DIR}/../../.." ABSOLUTE)
-set(RMW_DDS_COMMON_LIBRARY_DIR "${RMW_DDS_COMMON_PREFIX}/lib")
-get_filename_component(BACKWARD_ROS_PREFIX
-  "${backward_ros_DIR}/../../.." ABSOLUTE)
-set(BACKWARD_ROS_LIBRARY_DIR "${BACKWARD_ROS_PREFIX}/lib")
-
-ament_add_gtest(test_plugin test_plugin.cpp
-  APPEND_LIBRARY_DIRS
+if(APPLE)
+  find_package(rcl_logging_spdlog REQUIRED)
+  find_package(rmw_fastrtps_cpp REQUIRED)
+  find_package(rmw_dds_common REQUIRED)
+  find_package(backward_ros REQUIRED)
+  get_filename_component(RCL_LOGGING_SPDLOG_PREFIX
+    "${rcl_logging_spdlog_DIR}/../../.." ABSOLUTE)
+  set(RCL_LOGGING_SPDLOG_LIBRARY_DIR "${RCL_LOGGING_SPDLOG_PREFIX}/lib")
+  get_filename_component(RMW_FASTRTPS_CPP_PREFIX
+    "${rmw_fastrtps_cpp_DIR}/../../.." ABSOLUTE)
+  set(RMW_FASTRTPS_CPP_LIBRARY_DIR "${RMW_FASTRTPS_CPP_PREFIX}/lib")
+  get_filename_component(RMW_DDS_COMMON_PREFIX
+    "${rmw_dds_common_DIR}/../../.." ABSOLUTE)
+  set(RMW_DDS_COMMON_LIBRARY_DIR "${RMW_DDS_COMMON_PREFIX}/lib")
+  get_filename_component(BACKWARD_ROS_PREFIX
+    "${backward_ros_DIR}/../../.." ABSOLUTE)
+  set(BACKWARD_ROS_LIBRARY_DIR "${BACKWARD_ROS_PREFIX}/lib")
+  set(APPLE_TEST_LIBRARY_DIRS
     "${RCL_LOGGING_SPDLOG_LIBRARY_DIR}"
     "${RMW_FASTRTPS_CPP_LIBRARY_DIR}"
     "${RMW_DDS_COMMON_LIBRARY_DIR}"
     "${BACKWARD_ROS_LIBRARY_DIR}")
+endif()
+
+function(add_mujoco_gtest target source)
+  if(APPLE)
+    ament_add_gtest(${target} ${source}
+      APPEND_LIBRARY_DIRS ${APPLE_TEST_LIBRARY_DIRS})
+  else()
+    ament_add_gtest(${target} ${source})
+  endif()
+endfunction()
+
+add_mujoco_gtest(test_plugin test_plugin.cpp)
 target_link_libraries(test_plugin
   mujoco_ros2_control
   rclcpp::rclcpp
 )
 
-ament_add_gtest(test_headless_init test_headless_init.cpp
-  APPEND_LIBRARY_DIRS
-    "${RCL_LOGGING_SPDLOG_LIBRARY_DIR}"
-    "${RMW_FASTRTPS_CPP_LIBRARY_DIR}"
-    "${RMW_DDS_COMMON_LIBRARY_DIR}"
-    "${BACKWARD_ROS_LIBRARY_DIR}")
+add_mujoco_gtest(test_headless_init test_headless_init.cpp)
 target_link_libraries(test_headless_init
   mujoco_ros2_control
   rclcpp::rclcpp
   hardware_interface::hardware_interface
 )
 
-ament_add_gtest(test_viewer_camera test_viewer_camera.cpp
-  APPEND_LIBRARY_DIRS
-    "${RCL_LOGGING_SPDLOG_LIBRARY_DIR}"
-    "${RMW_FASTRTPS_CPP_LIBRARY_DIR}"
-    "${RMW_DDS_COMMON_LIBRARY_DIR}"
-    "${BACKWARD_ROS_LIBRARY_DIR}")
+add_mujoco_gtest(test_viewer_camera test_viewer_camera.cpp)
 target_link_libraries(test_viewer_camera
   mujoco_ros2_control
 )
 
-ament_add_pytest_test(test_urdf_to_mujoco_utils test_urdf_to_mujoco_utils.py
-  APPEND_LIBRARY_DIRS
-    "${RCL_LOGGING_SPDLOG_LIBRARY_DIR}"
-    "${RMW_FASTRTPS_CPP_LIBRARY_DIR}"
-    "${RMW_DDS_COMMON_LIBRARY_DIR}"
-    "${BACKWARD_ROS_LIBRARY_DIR}")
+if(APPLE)
+  ament_add_pytest_test(test_urdf_to_mujoco_utils test_urdf_to_mujoco_utils.py
+    APPEND_LIBRARY_DIRS ${APPLE_TEST_LIBRARY_DIRS})
+else()
+  ament_add_pytest_test(test_urdf_to_mujoco_utils test_urdf_to_mujoco_utils.py)
+endif()
diff --git a/mujoco_ros2_control_plugins/CMakeLists.txt b/mujoco_ros2_control_plugins/CMakeLists.txt
index ce86e1c..d1f1ee6 100644
--- a/mujoco_ros2_control_plugins/CMakeLists.txt
+++ b/mujoco_ros2_control_plugins/CMakeLists.txt
@@ -62,31 +62,35 @@ install(TARGETS mujoco_ros2_control_plugins
 
 if(BUILD_TESTING)
   find_package(ament_cmake_gtest REQUIRED)
-  find_package(rcl_logging_spdlog REQUIRED)
-  find_package(rmw_fastrtps_cpp REQUIRED)
-  find_package(rmw_dds_common REQUIRED)
-  find_package(backward_ros REQUIRED)
-  get_filename_component(RCL_LOGGING_SPDLOG_PREFIX
-    "${rcl_logging_spdlog_DIR}/../../.." ABSOLUTE)
-  set(RCL_LOGGING_SPDLOG_LIBRARY_DIR "${RCL_LOGGING_SPDLOG_PREFIX}/lib")
-  get_filename_component(RMW_FASTRTPS_CPP_PREFIX
-    "${rmw_fastrtps_cpp_DIR}/../../.." ABSOLUTE)
-  set(RMW_FASTRTPS_CPP_LIBRARY_DIR "${RMW_FASTRTPS_CPP_PREFIX}/lib")
-  get_filename_component(RMW_DDS_COMMON_PREFIX
-    "${rmw_dds_common_DIR}/../../.." ABSOLUTE)
-  set(RMW_DDS_COMMON_LIBRARY_DIR "${RMW_DDS_COMMON_PREFIX}/lib")
-  get_filename_component(BACKWARD_ROS_PREFIX
-    "${backward_ros_DIR}/../../.." ABSOLUTE)
-  set(BACKWARD_ROS_LIBRARY_DIR "${BACKWARD_ROS_PREFIX}/lib")
-
-  ament_add_gtest(test_external_wrench_plugin
-    test/test_external_wrench_plugin.cpp
-    APPEND_LIBRARY_DIRS
+  if(APPLE)
+    find_package(rcl_logging_spdlog REQUIRED)
+    find_package(rmw_fastrtps_cpp REQUIRED)
+    find_package(rmw_dds_common REQUIRED)
+    find_package(backward_ros REQUIRED)
+    get_filename_component(RCL_LOGGING_SPDLOG_PREFIX
+      "${rcl_logging_spdlog_DIR}/../../.." ABSOLUTE)
+    set(RCL_LOGGING_SPDLOG_LIBRARY_DIR "${RCL_LOGGING_SPDLOG_PREFIX}/lib")
+    get_filename_component(RMW_FASTRTPS_CPP_PREFIX
+      "${rmw_fastrtps_cpp_DIR}/../../.." ABSOLUTE)
+    set(RMW_FASTRTPS_CPP_LIBRARY_DIR "${RMW_FASTRTPS_CPP_PREFIX}/lib")
+    get_filename_component(RMW_DDS_COMMON_PREFIX
+      "${rmw_dds_common_DIR}/../../.." ABSOLUTE)
+    set(RMW_DDS_COMMON_LIBRARY_DIR "${RMW_DDS_COMMON_PREFIX}/lib")
+    get_filename_component(BACKWARD_ROS_PREFIX
+      "${backward_ros_DIR}/../../.." ABSOLUTE)
+    set(BACKWARD_ROS_LIBRARY_DIR "${BACKWARD_ROS_PREFIX}/lib")
+    set(APPLE_TEST_LIBRARY_DIRS
       "${RCL_LOGGING_SPDLOG_LIBRARY_DIR}"
       "${RMW_FASTRTPS_CPP_LIBRARY_DIR}"
       "${RMW_DDS_COMMON_LIBRARY_DIR}"
-      "${BACKWARD_ROS_LIBRARY_DIR}"
-  )
+      "${BACKWARD_ROS_LIBRARY_DIR}")
+    ament_add_gtest(test_external_wrench_plugin
+      test/test_external_wrench_plugin.cpp
+      APPEND_LIBRARY_DIRS ${APPLE_TEST_LIBRARY_DIRS})
+  else()
+    ament_add_gtest(test_external_wrench_plugin
+      test/test_external_wrench_plugin.cpp)
+  endif()
   target_link_libraries(test_external_wrench_plugin
     mujoco_ros2_control_plugins
     ${mujoco_ros2_control_msgs_TARGETS}
