diff --git a/mujoco_ros2_control/include/mujoco_ros2_control/mujoco_system_interface.hpp b/mujoco_ros2_control/include/mujoco_ros2_control/mujoco_system_interface.hpp
index c1cc93e..a887d6c 100644
--- a/mujoco_ros2_control/include/mujoco_ros2_control/mujoco_system_interface.hpp
+++ b/mujoco_ros2_control/include/mujoco_ros2_control/mujoco_system_interface.hpp
@@ -369,6 +369,7 @@ private:
 
   // True when running without a display (no GLFW window)
   bool headless_{ false };
+  bool disable_rendering_{ false };
 
   // Primary simulate object
   std::unique_ptr<mujoco::Simulate> sim_;
diff --git a/mujoco_ros2_control/src/mujoco_system_interface.cpp b/mujoco_ros2_control/src/mujoco_system_interface.cpp
index f2ed0f2..cb1e749 100644
--- a/mujoco_ros2_control/src/mujoco_system_interface.cpp
+++ b/mujoco_ros2_control/src/mujoco_system_interface.cpp
@@ -820,6 +820,9 @@ MujocoSystemInterface::on_init(const hardware_interface::HardwareComponentInterf
   // Check for headless mode
   headless_ = hardware_interface::parse_bool(get_hardware_parameter(get_hardware_info(), "headless").value_or("false"));
   RCLCPP_INFO_EXPRESSION(get_logger(), headless_, "Running in HEADLESS mode.");
+  disable_rendering_ =
+      hardware_interface::parse_bool(get_hardware_parameter(get_hardware_info(), "disable_rendering").value_or("false"));
+  RCLCPP_INFO_EXPRESSION(get_logger(), disable_rendering_, "Camera and lidar rendering is disabled.");
 
   // We essentially reconstruct the 'simulate.cc::main()' function here, and
   // launch a Simulate object with all necessary rendering process/options
@@ -1402,9 +1405,14 @@ hardware_interface::CallbackReturn MujocoSystemInterface::on_activate(const rclc
 {
   RCLCPP_INFO(get_logger(), "Activating MuJoCo hardware interface and starting Simulate threads...");
 
-  // Start camera and sensor rendering loops
-  cameras_->init();
-  lidar_sensors_->init();
+  // Start camera and sensor rendering loops unless explicitly disabled. This
+  // also provides a graphics-free headless path on platforms where GLFW must
+  // create windows from the process main thread.
+  if (!disable_rendering_)
+  {
+    cameras_->init();
+    lidar_sensors_->init();
+  }
 
   return hardware_interface::CallbackReturn::SUCCESS;
 }
diff --git a/mujoco_ros2_control/tests/test_headless_init.cpp b/mujoco_ros2_control/tests/test_headless_init.cpp
index 6212cc7..a3fcef6 100644
--- a/mujoco_ros2_control/tests/test_headless_init.cpp
+++ b/mujoco_ros2_control/tests/test_headless_init.cpp
@@ -245,6 +245,20 @@ TEST_F(HeadlessInitTest, HeadlessInitialization)
   EXPECT_GE(state_interfaces.size(), 0);
 }
 
+TEST_F(HeadlessInitTest, RenderingDisabledActivationDoesNotInitializeGraphics)
+{
+#if ROS_DISTRO_HUMBLE
+  ASSERT_EQ(interface_->on_init(hardware_info_), hardware_interface::CallbackReturn::SUCCESS);
+#else
+  hardware_interface::HardwareComponentInterfaceParams params;
+  params.hardware_info = hardware_info_;
+  ASSERT_EQ(interface_->on_init(params), hardware_interface::CallbackReturn::SUCCESS);
+#endif
+
+  rclcpp_lifecycle::State inactive_state(0, "inactive");
+  EXPECT_EQ(interface_->on_activate(inactive_state), hardware_interface::CallbackReturn::SUCCESS);
+}
+
 TEST_F(HeadlessInitTest, PauseTrueSnapshotUsesAuthoritativeDataUnderMutexWithoutGenericUpdateOrPhysicsWrites)
 {
 #if ROS_DISTRO_HUMBLE
