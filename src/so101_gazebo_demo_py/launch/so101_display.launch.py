"""Rejected legacy launcher with no equivalent unified composition."""


def generate_launch_description():
    raise RuntimeError(
        "so101_display.launch.py cannot be mapped to the unified application launcher"
    )
