#pragma once

#include <pick_place_common/workflow_definition.hpp>

namespace so101_gazebo_demo::pick_place
{
[[nodiscard]] const pick_place_common::WorkflowDefinition & so101WorkflowDefinition();
}  // namespace so101_gazebo_demo::pick_place
