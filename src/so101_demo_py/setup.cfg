[develop]
script_dir=$base/lib/so101_demo_py
[install]
install_scripts=$base/lib/so101_demo_py

[tool:pytest]
testpaths =
    test
addopts = -m "not explicit_ml"
markers =
    explicit_ml: opt-in Torch/SAM integration tests excluded from the ordinary full run
